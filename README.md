# Agenda Financeira — GiuliaNeves

Dashboard financeiro pessoal com Python (Flask) + HTML/CSS/JS.

## Como rodar
```bash
cd agenda-financeira
python -m venv venv
source venv/bin/activate        # Windows: venv\Scripts\activate
pip install -r requirements.txt
python app.py